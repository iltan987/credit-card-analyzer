# Environment Variables Configuration

# Vercel Blob Storage (only needed if using DATA_SOURCE=blob)
BLOB_READ_WRITE_TOKEN=your_vercel_blob_token_here
VERCEL_OIDC_TOKEN=your_vercel_oidc_token_here

# Data Source Configuration
# Controls where the application loads financial data from:
# - 'blob' = Vercel Blob storage (requires BLOB_READ_WRITE_TOKEN)
# - 'local' = Local data folder (default)
# - Leave empty or undefined = defaults to 'local'
DATA_SOURCE=local

# Example configurations:
# DATA_SOURCE=blob   # Use Vercel Blob storage
# DATA_SOURCE=local  # Use local data folder (default)
# DATA_SOURCE=       # Empty/undefined defaults to local
